<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="User">
    <insert id="insert" parameterType="userDTO">
        insert into trade_user_table (userId, userPassword, userName, userMobile)
        values (#{userId}, #{userPassword}, #{userName}, #{userMobile})
    </insert>

    <select id="login" resultType="userDTO">
        select *
        from trade_user_table
        where userId = #{userId}
          and userPassword = #{userPassword}
    </select>

    <select id="myPage" resultType="userDTO">
        select *
        from trade_user_table
        where userId = #{userId}
    </select>

    <update id="update" parameterType="userDTO">
        update trade_user_table
        set userPassword = #{userPassword},
            userName     = #{userName},
            userMobile   = #{userMobile}
        where id = #{id}
    </update>
</mapper>














